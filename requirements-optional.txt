# MACCREv2 OPTIONAL DEPENDENCIES
# These are only installed if local configuration requires failover models.
# They are fundamentally not required to run the core swarm loops.

openai>=1.14.0
anthropic>=0.20.0
groq>=0.4.0
ollama>=0.1.7
