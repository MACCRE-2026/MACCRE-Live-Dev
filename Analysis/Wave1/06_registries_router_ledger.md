# Complete Granular Functional Ledger: MACCRE Root Registries & Core Components

**Target Analysis File Path**: `B:\EXO_GANS\Analysis\Wave1\06_registries_router_ledger.md`
**Analysis Target Modules**: `maccre_core/` root components:
- `maccre_router.py` (38,632 bytes, 751 lines)
- `controlnode_registry.py` (20,727 bytes, 530 lines)
- `macronode_registry.py` (16,025 bytes, 386 lines)
- `collection_registry.py` (10,887 bytes, 269 lines)
- `workbook_data.py` (10,432 bytes, 212 lines)
- `agent_library.py` (8,880 bytes, 214 lines)
- `topology_library.py` (9,057 bytes, 229 lines)
- `drive_watcher.py` (20,110 bytes, 414 lines)
- `logger.py` (22,887 bytes, 552 lines)

---

## 1. Executive Summary & Architectural Overview

The `maccre_core` component suite forms the sovereign execution backbone and state registry layer of the Google Antigravity / MACCREv2 architecture. Operating under **Engineering Doctrine Law Rev 19.0**, all modules strictly enforce Python 3.11+ static typing, zero-dependency standard library fallback routing, 5-Tier Data Sovereignty directory layout, and zero-leak resource teardown.

This ledger documents the line-by-line functional mechanics of:
1. **Universal & Agent Inference Routing** (`maccre_router.py`)
2. **Deterministic Control Primitive Registry** (`controlnode_registry.py`)
3. **MacroNode & Ephemeral Topology Store** (`macronode_registry.py`)
4. **RAG Vector & Collection Ingestion Registry** (`collection_registry.py`)
5. **Global Roster & Swarm Libraries** (`agent_library.py` & `topology_library.py`)
6. **Live System State Loader & Workbook Bridge** (`workbook_data.py`)
7. **Sovereign Drive Watcher Daemon** (`drive_watcher.py`)
8. **Dual-Channel Operations Logging & Telemetry Subsystem** (`logger.py`)

---

## 2. Universal & Agent Routing Architecture (`maccre_router.py`)
Dual-surface LLM dispatch engine bridging cloud APIs (Gemini, Claude, OpenAI, Groq), local air-gapped runners (Ollama, Edge S25), tiered failover chains, context caching, and structured Pydantic schema enforcement.

## 3. Deterministic Flow Primitive Registry (`controlnode_registry.py`)
SQLite-backed registry managing 25 deterministic non-AI pipeline nodes across 7 functional categories with auto-seeding and local broker HITL interception hooks.

## 4. Saved Topology & Ephemeral Graph Persistence (`macronode_registry.py`)
Unified persistent store for template/fixed swarm topologies and runtime ephemeral node state.

## 5. RAG Vector & Collection Ingestion Registry (`collection_registry.py`)
Document ingestion pipeline integrating file fingerprinting, multi-parser routing, parsed artifact isolation, and vector memory synchronization.

## 6. Global Roster & Swarm Libraries (`agent_library.py` & `topology_library.py`)
SQLite stores implementing dual-silo persistence across project-local and GLOBAL Datacenter boundaries.

## 7. Live System State Loader & Workbook Bridge (`workbook_data.py`)
Dynamic data loader surfacing live system models, projects, agent rosters, topologies, and operator-facing tool dispatchers.

## 8. Sovereign Drive Watcher Daemon (`drive_watcher.py`)
File system watcher supporting `*_APPROVED.xlsx` recursive monitoring, 7-stage automated swarm materialization/execution, auto-nugget export, and workbook archival.

## 9. Dual-Channel Operations Logging & Telemetry Subsystem (`logger.py`)
Operations logging engine supporting JSON console formatting, human op-logs, in-place log rotation, dual-channel SQLite event logging, and bifurcated `FlowChat`/`FlowSystem` JSONL exhaust ledgers.
