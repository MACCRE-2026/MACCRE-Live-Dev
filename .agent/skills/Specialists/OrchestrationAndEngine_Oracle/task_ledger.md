# Continuous Subsystem Task & Change History Ledger: Orchestration & Engine Oracle

**Specialist Domain:** `maccre_core/orchestration/` (`deterministic_nodes.py`, `flow_engine.py`, `swarm_worker.py`, `local_broker.py`, `macro_factory.py`, `dialogue_runner.py`, `topology_engine.py`)  

---

## Task & Change History Log

- **2026-07-24 (Initialization)**: Specialist Oracle profile established. Initial analysis artifacts compiled in `Analysis\Wave1\02_engine_swarms_ledger.md`, `03_orchestration_factory_ledger.md`, and `Analysis\Wave2\flowchart_02_orchestration_engine.md`. Core swarm engine, deterministic primitives, SQLite WAL scatter-gather queue, and topology validator verified.
- **2026-07-25 (Documentation Audit & Master Rewrite)**: Completed Domain 2 documentation audit and drafted Orchestration subsystem sections in `Analysis\Wave3\draft_orchestration_docs.md`. Synthesized findings into production [README.md](file:///b:/EXO_GANS/README.md) and [MACCRE_Operator_Manual.md](file:///b:/EXO_GANS/MACCRE_Operator_Manual.md). Documented 16 `CTRL_` control nodes, Quadrivector failback routing, SQLite WAL scatter-gather queue mechanics, and 7-point pre-flight topology validation protocol.
- **2026-07-25 (Phase 9 Orchestration Architecture Review)**: Authored Domain 2 contribution for Phase 9 of `Era3_architectural_roadmap.md` in `B:\EXO_GANS\.oracle_artifacts\draft_era3_phase9_orchestration.md`. Defined `MacroNodeCompiler` and `LocalSwarmHarness`.
- **2026-07-28 (CTRL_SCATTER Expansion Plan v1-v3 Orchestration Review)**: Conducted deep domain audit of `ctrl_scatter-expansion plan-v1.md`, `v2.md`, and `v3.md` across `flow_engine.py`, `swarm_worker.py`, `local_broker.py`, and `topology_engine.py`. Synthesized comprehensive architectural report in `b:\EXO_GANS\.oracle_artifacts\2026-07-28_ctrl_scatter_review_orchestration.md`. Uncovered 5 critical engine/hydration bugs (missing `step_config` at L545, `_hydrate_topology` comma-separated `Next_Node` suffix bug, missing `Tether_ID` in dynamic topology rows, preflight agent validation bypass, and missing local model hardware probing). Approved v3 telemetric `flow_vector` schema groundwork and Phase 6 WAL sharding architecture.
- **2026-07-28 (Phase 4.75.7 & Era 2 Roadmap Audit)**: Conducted domain audit of Phase 4.75.7 and Era 2 Architectural Roadmap across `flow_engine.py`, `swarm_worker.py`, `local_broker.py`, `telemetry_db.py`, and `Era2_architectural_roadmap.md`. Verified 100% completion of Phase 4.75.7 deliverables (`Tether_ID` auto-wrap, comma-split next_node target hydration, `step_config` passthrough, preflight validation bypass, `flow_vector` `>` lineage tracking, tether-scoped fan-in artifact collection, and `system_logs` schema columns). Confirmed all historical items (Phases 1–4.75.6) are complete and future deferrals (Phases 6 & 7) are cleanly mapped in `2026-07-28_phase4_75_7_roadmap_audit_orchestration.md`.
- **2026-08-09 (Phase 4.99 Comprehensive Subsystem Audit)**: Executed comprehensive domain audit of `maccre_core/orchestration/` (`swarm_worker.py`, `flow_engine.py`, `deterministic_nodes.py`, `local_broker.py`, `macro_factory.py`, `dialogue_runner.py`, `topology_engine.py`) in context of Phase 4.99 user testing needs (8-agent scatter bursts, tethering, scatter auto-wrap DAG synthesis, `flow_vector` lineage logging, `flow_line_id` routing, Quadrivector failback, and worker process recovery). Discovered critical broker bug (`tether_id` parameter missing from `LocalMessageBroker.route_task()` signature and storage layer SQL, breaking tether-scoped fan-in artifact collection), missing zombie task lock timeout reclaim, and `ROUTE_TO:` fuzzy Levenshtein alignment gap. Authored full report artifact in `b:\EXO_GANS\.oracle_artifacts\2026-08-09_OrchestrationAndEngine_Oracle_phase4_99_audit.md` and mapped all items into past, Phase 4.99, and future roadmap phases.

- **2026-08-30 (Phase 6.12 Parallel Agent Execution — Rebuild & Closeout)**: Rebuilt Phase 6.12 from scratch on the Aug 22 rollback baseline (`f7b326f`) after the Aug 28 attempt was reverted. Delivered genuine concurrent scatter execution via a new demand-scaled `DynamicSwarmPool` (0 → N → 0) in `maccre_core/orchestration/swarm_pool.py` and a new `maccre_core/orchestration/concurrency.py` (thread-routed stdout/stderr, per-path locks, atomic writes, `MAX_SCATTER_AGENTS`/`SCATTER_HARD_CAP`, sliding-window provider `RateLimiter`). Replaced the two duplicated `for _ in range(500): worker.execute_cycle()` loops in `flow_engine.py` with one shared `_run_worker_pool` driver used by both `execute_flow` and `resume_flow`. Removed the `CTRL_REVIEW` hardcode from all three sites in favour of registry-driven resolution (`deterministic_nodes.NODE_ALIASES` + `resolve_primitive_node_id`), and fixed the `controlnode_registry` seed row that named a handler (`local_broker.intercept_review_via_route_task`) which has never existed anywhere in the repo. Gave `execute_cycle` an observable `CycleOutcome` (WORKED/IDLE/PAUSED/STOPPED) plus paired per-node lifecycle callbacks, and wired those through `nexus_plex` into a multi-active `TopologyVisualizer` (`mark_node_active`/`mark_node_finished`) and an `N/8 active` flow-monitor readout. **8-way concurrency is proven deterministically** by a `threading.Barrier(8)` width proof in `tests/test_scatter_concurrency.py`, which can only complete if all eight lanes are in flight simultaneously. Fixed 14 defects, **9 of them pre-existing and latent** because sequential execution had hidden them — most severely a shared SQLite connection that made `BEGIN EXCLUSIVE` isolate nothing (measured: 15 claims for 12 tasks, i.e. three tasks handed to two workers each, plus three threads dead with "cannot start a transaction within a transaction"), a torn unified-session-ledger write on the path that feeds the next node's input payload, context-cache registry loss (measured: 8 uploads for one identical payload and 1 of 8 entries surviving), a global `sys.stdout` swap that *was* the entire agent-logging path via `DynamicStreamHandler`, and step config being silently discarded after the 5 s `TopologyEngine` cache TTL. Also corrected two false claims in the rollback record: the `CTRL_REVIEW` hardcode was neither introduced by 6.12 nor removed by the rollback (the verified baseline passed with it present), and `flow_vector` telemetry is not populated, so no success criterion can be phrased in terms of it. Caught and fixed one regression this phase introduced: removing the review preflight bypass exposed a latent `TopologyEngine.validate()` rule demanding a prompt and model of every node, which hard-blocked launch for any control-node flow and would have blocked the Phase 4.99 certification shape. Full gate green — `omni qa` (whole project), **460/460 pytest** (up from **zero runnable**, since one orphaned module had been aborting collection repo-wide), and `omni smoke`. Artifacts: `.oracle_artifacts/2026-08-29_phase_6_12_ctrl_review_baseline.md` (the machine-checkable baseline trace) and `.oracle_artifacts/2026-08-30_phase_6_12_parallel_execution_closeout.md` (full closeout, state-contract table, accepted `pause_event` ownership inversion, and the Phase 6.13 gate criteria). Authored `.kiro/steering/omni_pipeline_mandate.md` (always-applied) after `omni qa` caught three defects that scoped lint/type invocations had missed. Phase 6.13 (WAL sharding) remains **gated**: requires 3 consecutive clean 8-agent runs, zero `database is locked` in telemetry, and confirmed peak concurrency ≥ 8. Recorded `local_broker.reclaim_zombie_locks` as uncalled and unsafe (age test keys off `created_at`, not lock acquisition — there is no `locked_at` column — so it would double-execute nodes at 8-wide scatter); do not wire it in without adding that column first.

- **2026-09-01 (Defects E1 & E2 — Payload Lineage at Two Handoffs)**: Fixed the two payload-lineage defects left open by the Phase 4.99 handover, both of a single failure class — *an artifact losing its identity at a handoff* — and both found by reading an agent's ledger on live run `job_20260831-041428-6goe` rather than by the 665-test suite, because both lived in seams no stubbed test visits (principle 6, second demonstration this phase). **E1**: every scatter lane's queue row reported `payload_path = unified_session_ledger.md`, so `get_completed_payload_paths` returned one path eight times and `_handle_merge` logged `Merged 8 sources` over eight identical `## Source: unified_session_ledger` headings — literally true, semantically empty. **The handover attributed this to `route_task` overwriting the completing row and suggested an `output_path` column; that was directionally right and mechanically wrong.** The lane's own path is already discarded one layer earlier, in `swarm_worker.execute_cycle`, where the Unified Ledger branch replaces `routing_payload_path` with the shared ledger — correct for routing, fatal as a record. A column alone would have faithfully stored the shared ledger. Fixed both: `task_queue` gains `output_path` (CREATE TABLE body + ALTER migration, Track A `locked_at` idiom), `route_task` takes and records it at both UPDATE sites with `CASE WHEN ? = '' THEN output_path ELSE ? END` so no later caller can blank an earlier record, `get_completed_payload_paths` reads `COALESCE(NULLIF(output_path,''), payload_path)`, and the worker now captures `node_output_path` *before* the swap. The fallback is load-bearing rather than a legacy shim: `macro_factory`'s spawn/FAILED routes and the `CTRL_PAUSE` resolver legitimately never supply an output because for them the routing payload *is* the output. Also deleted the query's docstring, which stated the falsified assumption ("the completed row is the authoritative record of what that node produced") directly above the SELECT it justified — principle 5, a load-bearing claim with no test to fail when it went false. **E2**: the merge row correctly pointed at `CTRL_MERGE_S0_merged.md` (426 KB) while the next step was queued with the merge's 59-byte ledger stub `CTRL_MERGE_S0_93.md`, at a billed $0.000046. `_find_final_ledger_path` globbed the job's ledger directory by mtime, and because the worker writes the stub *after* `_handle_merge` returns, **this was not a race it sometimes lost but one it always lost**; it also accepted `topology_rows` and never read them, and scoped to the job rather than the step. Deleted it outright (two ways to answer one question is how the TUI and engine came to disagree about node ids) and replaced it with `_find_terminal_nodes` — the mirror of `_find_starting_nodes`, reading `topology_graph.terminal_nodes()` and hydrating through the same `f"{node}_S{idx}"` expression — plus `_capture_step_output`, which asks the queue what that node recorded. **No silent fallback**: `None` leaves the payload unchanged and logs at ERROR, because retreating to the defective path would let the defect return with the suite green; failing the flow outright was rejected as the operator's call, being the same shape as the open `timeout` decision (which remains deliberately unhandled). Divergent terminals resolve by declared topology order, never completion time, and say so. Hardened `_handle_merge`/`_handle_concat` to de-duplicate inputs and report a collapse loudly, redundant after the root-cause fix but kept because the original failure was *silent* (principle 3). Added `tests/test_payload_lineage.py` (38 tests, including the first assertions in this repo about merged *content*, and source-order guards for the one-line ordering whose cost is only observable through a live model call). **Both headline tests were verified by reverting each fix and confirming they fail with the exact live signature** — E1 revert produced eight lanes all reporting `unified_session_ledger.md`; E2 revert selected `CTRL_MERGE_S0_93.md` over `CTRL_MERGE_S0_merged.md`, the same two filenames as the live run. Gate observed 2026-09-01: `omni qa` PASSED whole project, **703 collected / 703 passed** (665 + 38), `omni smoke` ALL CHECKS PASSED. **Recorded against the handover: the untouched baseline was 664/665, not 665/665** — `test_eight_lane_scatter_beats_sequential_wall_clock` is a load-sensitive wall-clock bound (1.70 s against 1.20 s) that passes in isolation and 32/32 in its own file, with the companion `peak == 8` barrier test passing in the full run, so width is intact; left untouched as an operator decision. **NOT VERIFIED: no live 8-lane scatter was run.** Static analysis, 703 unit tests and a single-node smoke are not a live run, and this is precisely the combination that was green while six real defects sat in this path. E1/E2 status is *fixed and gated, not reproduced as fixed in production*; UT-0 runs 2–3 and UT-1 (especially test 6) remain the proof, with the acceptance shape being eight **distinct** `## Source:` sections naming eight different lanes and the following step receiving that document. Also recorded for the operator, outside this domain: `tests/`, `Analysis/`, `scripts/` and `.oracle_artifacts/` are excluded by `.git/info/exclude` as a deliberate public/private split, so the test suite has **no version control at all** — which widens risk R8 rather than closing it, and is not a unilateral change to make since reversing it would publish private internals to a public remote. Artifact: `.oracle_artifacts/2026-09-01_e1_e2_payload_lineage.md`. Branch `phase/6.13-track-a-d-and-payload-lineage`, on checkpoint commit `762f614`.

- **2026-09-01 (Defects F1/F2/F3 — The Pause Path, plus the timeout decision closed)**: Three defects from one keypress, all found by operator live testing rather than by the suite. The operator ran two deliberately-different 8-agent scatters — `job_20260901-204957-ico6` **cancelled** mid-run and `job_20260901-205047-40sp` **paused** mid-run. Cancel was correct (9 completed / 1 cancelled, no orphans — effectively **UT-1 test 5 passing**). Pause produced a dead UI, a process at 257 s of CPU, and a run one hour of budget away from reporting success over an unexecuted merge. **F1**: `MacroNodeWorkshop.DEFAULT_CSS` pinned the VCR button to `min-width/max-width: 4`; all three `.vcr-btn--*` rules declare `border: solid` (2 cells) and Textual's `Button` carries `padding: 0 1` (2 cells), so the content box was **zero**, and `rich._wrap.divide_line` → `chop_cells(word, 0)` evaluates `range(0, n, 0)` and raises `ValueError: range() arg 3 must not be zero` out of the render, killing the Textual app while the engine thread carried on. Fixed to 6 with the arithmetic table recorded beside the number. **`max-width` was the property that bit** — `min-width` alone would have grown harmlessly. Two hypotheses tried and discarded, both pinned by tests: the glyphs are *not* wide (both `⏸` U+23F8 and `▶` U+25B6 measure 1 cell via `rich.cells.cell_len`, so changing the label would have left the crash in place), and **the `maccre_tui` pyright exclusion is NOT why this escaped** — I asserted that earlier in the session and withdrew it, because a zero width is a well-typed `int` and no type checker catches a value a library divides by. Establishing *which* `#btn-vcr` was live was the step that made it conclusive: there are two, and the other belongs to `FlowExecutionPanel`, which is **defined and never instantiated** and whose copy inherits a safe `min-width: 8` (now its own register entry; not deleted this pass). **F2**: `_worker_loop` folded `PAUSED` into `IDLE`'s retire branch on the reasoning "the supervisor re-spawns when demand returns, so retiring eagerly is cheap" — true for IDLE, inverted for PAUSED, because demand is counted from *open* rows and a paused task is still open. So a held flow retired and rebuilt a full worker (`TopologyEngine` + `LocalMessageBroker` running schema DDL against the very file a claim needs) every supervisor tick. **Measured by reverting each half independently: both broken → 40 constructions in 2 s at a 0.01 s tick and unbounded; worker-hold only → exactly `max_workers` (8), then bounded by the ceiling check; both fixed → ≤ 2.** Either half alone prevents the runaway; the scaler gate is load-bearing. Fixed with three changes plus a single `_is_paused()` seam for the inverted convention (**set means running**): the scaler returns before paying for the demand query while held, `PAUSED` holds its slot for `pause_hold_seconds`, and the supervisor poll backs off 0.05 → 0.25 s since the drain check is a SQLite `COUNT` contending with claims. **Recorded for its own sake: `test_a_cleared_pause_event_holds_execution` already covered this path and passed throughout, because it asserted no *work* executed and never asserted what the pause *cost*** — principle 6 in one test, a correct assertion on the wrong axis. **F3**: `pause_event` is TUI-owned, so when F1 killed the app with it clear, nothing could ever set it again; the pool waited out `timeout_seconds=3600`, returned `"timeout"`, and **neither step loop branched on `timeout`** — it fell through to the payload capture, the step logged complete, and the `finally` wrote `completed`. Nothing was hidden; the status was in `_run_worker_pool`'s own docstring and simply had no branch. Closed both halves: the pool now **asks** rather than guesses, via an optional `pause_owner_alive` predicate consulted *only while held*, with `nexus_plex` supplying `lambda: self.is_running` at both call sites (the app being the only party with standing to answer); and both loops now break on `("stalled", "timeout", "abandoned")` as one membership test, since the two loops have drifted before. New `PoolResult.pause_abandoned` and a distinct `"abandoned"` status because "your UI died" and "this node is slow" need different responses. `max_pause_seconds` exists as a backstop for callers with no liveness signal and is **off by default deliberately** — killing a flow because the operator went to lunch would be worse than the defect. **This closes the register entry *A timed-out step does not stop the flow*, held at `Deferred (needs decision)` since 2026-08-30 and authorised by the operator in session**, on the explicit understanding that it will fail flows which currently report success. Two labels corrected while there: `is_stalled = True` was being set for a timeout (an approximately-correct identifier — a future reader would have believed a timed-out session stalled), now `unfinished_as = pool_status` with the reason logged in the `finally`; and `_wait_for_hitl_resume` returned a `bool` meaning *cancelled* **or** *timed out* **or** *no pause channel existed*, now returning `_run_worker_pool`'s own vocabulary, with `pause_event=None` reported as `abandoned` rather than `timeout` because nothing can ever release it. **Accepted cost, recorded rather than glossed:** `failed` now covers four conditions (exception, stall, timeout, abandoned) and `job_sessions` has no reason column, so the distinction lives only in the log — accepted because a wrong `failed` is conservative while a wrong `completed` propagates, and a reason column is a schema *and* contract change belonging with the File Cabinet read API. **Also recorded the Drive-vs-SQLite concurrency cut**, which existed only as a comment: `hybrid_edge_sync.py` claimed to be "formerly `datacenter_router.py`" with Drive hot-locking "permanently retired", but that module is still present, still contains the working Drive `appProperties` locking, and is imported by nothing — so a reader lands on live-looking dead code, which happened in this session. `datacenter_router.py` now carries a `SUPERSEDED` header and is **retained deliberately** as the reference implementation for the planned Drive *transport* layer (transport over Drive is still the plan; locking over Drive is not); the false "Formerly" claim is corrected. **And a clarification worth keeping**: the F2 runaway was initially described as semi-intentional, a loop holding a socket open for an unexpected edge-node join. It was not and could not be — `_worker_loop` builds and destroys *local in-process* workers and there is no listener, port or registration table in the pool. **The join mechanism is the queue row**: `lock_status = 'open'` is claimable by anything that can reach the database and issue `BEGIN EXCLUSIVE`, which is exactly what the retirement note says won. `CTRL_MERGE_S0` sat open and claimable throughout the runaway; the spin added nothing and contended with it. Gate observed 2026-09-01: `omni qa` PASS whole project, **750 collected / 750 passed** (703 before), `omni smoke` ALL CHECKS PASSED. All three fixes **reproduction-verified** by reverting each and confirming the new tests fail with the production signature, F1's down to the identical `rich/cells.py:338` frame from the operator's traceback. Two pre-existing tests updated rather than worked around, each with a dated note (`TestHitlResumeGate`'s four bool assertions → status assertions; `test_a_stalled_step_marks_the_session_failed` from the literal branch to the membership form). One of my own new tests was wrong on first run — it pre-set `pause_event`, which `_wait_for_hitl_resume` immediately clears — worth recording because a test that ignores the ownership inversion tests nothing. **NOT VERIFIED, and it is short:** nobody has pressed pause in a live TUI since the fix. F1 is verified as arithmetic, not pixels; F2 against a stub worker that does not build a real broker; and F3's two-line `nexus_plex` wiring is covered by **no test at all**, being outside both the pyright include list and the suite's reach — and those two lines are what make the mechanism live. **E2 is still unproven** and is unaffected by this work: the merge never ran in either session. First live evidence that **E1 *is* fixed**, though: both runs show eight lanes with eight distinct `output_path` values naming their own ledgers (523 B – 22.7 KB on disk) while `payload_path` is uniformly the shared session ledger, and tether `scatter_84fe89ba` on all eight lanes and the merge. Artifact: `.oracle_artifacts/2026-09-01_f1_f2_f3_pause_path.md`. Two SOPs raised to the Sovereign Importer team: the payload-lineage one, and an amendment withdrawing the `completed`-is-not-proof caveat while widening `failed` from two conditions to four.

- **2026-09-03 (Paranoia Mode — honest disable, and a hard portability blocker removed from the topology loader)**: `maccre_core/utils/secret_auth.py` advertised *"Air-Gap Steganographic Hardware Authentication… Uses NTFS Alternate Data Streams and Hardware tokens"* **in the present tense** while `is_topology_approved()` was already `return True`. **Three separate places asserted a security control that did not exist** — that module docstring, `pattern_executor`'s, and `Analysis/Wave1/05_memory_schemas_utils_ledger.md`. Doctrine 5, and the same shape as the doctrine's own `--smart` incident, but worse in kind: a reader concludes topologies are hardware-gated when nothing gates them. **The larger cost was structural, and it was in this domain.** `secret_auth` imported `ctypes.wintypes` at **module scope**, which cannot import on a non-Windows host, and `topology_engine._pull_from_csv` imported it **unguarded** (`pattern_executor` guards its own import and degrades). So the topology loader — on **every execution path in the system** — was Windows-only, in service of a gate that always returned `True`. That is a hard Android blocker of the same class as the DPAPI credential vault, it had **no register entry at all**, and unlike the vault it cost nothing to remove because nothing depended on the value. Fixed by naming the capability **Paranoia Mode** and disabling it *honestly* rather than deleting it: `PARANOIA_MODE_ENABLED` is the single seam, all Win32 imports are lazy, the docstring leads with `CURRENTLY DISABLED`, and two new predicates resolve an ambiguity that mattered — `is_paranoia_mode_enabled()` exists because a caller receiving `True` from `is_topology_approved` could not distinguish *approved* from *not checked*, which is Doctrine 3's rule against folding an ambiguous state into a success, applied to an authorisation answer. `has_auth_stamp()` returns **`False` off Windows, not `True`**: a check that cannot be performed has not passed, and the permissive alternative is exactly the approximately-correct value Principle 2 exists for. Removed `topology_engine`'s import and its **unreachable** `PermissionError(f"DENIED: Topology … lacks Hardware Auth Stamp.")`, recording in the docstring where the enforcement point returns if the gate is revived; rejected the alternative of guarding the import behind `sys.platform`, since that retains the coupling for a no-op and still reads as a live gate. Renamed `pattern_executor._sign_topology` → `_record_topology_hash` and **deleted the unread ADS write** along with its `ADS auth stamp written` log line — its docstring had claimed the write happened *"so TopologyEngine.is_topology_approved() always returns True"*, which was false in a way worth naming, since the function returned `True` regardless and the write was therefore dead work under a log line implying a security step. Kept the SHA-256 content hash: platform-neutral, a genuine audit record, and what a future Paranoia Mode would stamp over. **Pre-work protocol finding:** none of the four mandatory domain artifacts documents the auth gate as load-bearing — `Wave2`'s pre-flight flowchart enumerates seven validation points and `MASTER_FLOWCHART`'s `FE_CHECKS` six, and the auth stamp is in neither, so removing it contradicts nothing documented. **Also noticed and recorded rather than fixed:** `Wave2`'s broker section documents a `tasks` table with `task_id`/`node_id`/`status`/`wait_for` plus a `task_deps` table, while the real table is `task_queue` with `id`/`job_id`/`current_node`/`lock_status` — a **seventh** representation of the queue schema, this one in documentation, alongside the six found on disk earlier the same day. Added `tests/test_paranoia_mode.py` (17 tests), whose load-bearing member is `test_docstring_cannot_drift_from_the_flag`: it asserts the docstring says `CURRENTLY DISABLED` **if and only if** the flag is `False`, so flipping the flag without rewriting the prose fails and rewriting the prose while the flag is off fails too — **the test whose absence caused the original defect.** Two assertions deliberately read the AST rather than executing, and the trade is stated in the test module: the failure mode is an import-time *platform* break that cannot be reproduced on a Windows host, so an AST check is weaker than execution but stronger than the nothing that existed. **Revert-to-red performed and restored on both**: `PARANOIA_MODE_ENABLED = True` reddened the docstring-drift test; restoring the module-scope `wintypes` import reddened the portability test. Gate observed 2026-09-03: `omni clean` (283 bytecode, 4 WAL/SHM), `omni qa` PASS whole project 22:15, **784 collected / 784 passed** 189.34s, `omni smoke` ALL CHECKS PASSED — smoke run because `topology_engine` is on an execution path. **NOT VERIFIED, and it is the whole point of the defect:** the portability fix has not been executed on a non-Windows host because none is available. "The loader now imports off Windows" rests on removing the only Windows-only module-scope import from the chain, verified by AST — treat it as *the blocker was removed*, not *portability is proven*. The linear-flow slot flake did not reproduce; **five clean full-suite runs now against one failure**, still unexplained, still blocking UT-0. Found incidentally while assessing an unrelated operator question about using Google Drive file headers as a laptop↔phone transport, which led into Alternate Data Streams and from there into this module. Artifact: `.oracle_artifacts/2026-09-03_paranoia_mode_honest_disable_and_portability.md`.

- **2026-09-04 (The demand estimator ramped to the ceiling for one task — reproduced, root-caused, fixed)**: The intermittent `test_linear_flow_stays_single_threaded` failure (`AssertionError: linear flow used slots {0, 1}`, one failure in six full-suite runs) was **neither a flake nor slot-id reuse**, and it was materially worse than the symptom suggested. `_scale_to_demand` computed `target = active + ready`, which **double-counts every live worker that has not claimed yet**: the worker is in `_active`, and the task it is about to take is still `open`, so it is still counted in `ready`. Because `active` grows while `ready` does not, **the target grew by one on every recheck interval** — a ramp toward `max_workers` for a single task, bounded only by the ceiling. Measured on a single-task queue with construction slower than the recheck: **5 workers spawned, 4 of which never claimed anything, 11 claim attempts for 1 task**, with the decision record reading `(0,1,True,1) (1,1,True,2) (2,1,True,3) (3,1,True,4) (4,1,True,5)`. After the fix: **1 worker, 0 wasted, 3 claim attempts.** Each wasted worker builds a `TopologyEngine` and a `LocalMessageBroker` that runs schema DDL against the very SQLite file the one real claim needs, so this is **defect F2's construction storm reached by a different route** — F2 was *paused, so demand stays high, so respawn*; this is *unclaimed, so demand stays high, so spawn more*. **F2's fix (the scaler returning early while paused) could not have caught this, and did not.** A stale-estimate guard already existed and its comment already recorded this exact symptom — *"Measured on a linear 3-step flow, which opened two threads for one task at a time"* — but it closes the window **inside** `demand_recheck_seconds` and not **at** the boundary, where a fresh estimate still sees the unclaimed task. The diagnosis was right and the fix was one case short, with nothing testing the remaining case. **Load-sensitive by construction**, which is why it read as a flake: worker construction is slower under load, so the spawned-to-claimed gap widens past the recheck interval under a full suite and does not when the test runs alone. **Instrumented before fixing**, per Principle 7, since the register had this as *observed, not reproduced* and explicitly *not distinguished from slot reuse*: added `PoolResult.workers_that_never_worked` (counting worker **instances** that never executed a node — which is what separates the defect from slot recycling, since a reused slot whose worker claims never registers) and `PoolResult.spawn_decisions`, a bounded record of `(active_before, ready, is_fresh, target)` at each scaling decision. That instrumentation is what turned "one spare thread, sometimes" into "five workers for one task, deterministically". **Fix**: `target = active + max(0, ready - unclaimed)` where `unclaimed = len(_active - _worked_slots)`, with `_worked_slots` added on `WORKED` and **discarded on retire** so a recycled slot starts unclaimed — without that discard the same defect returns with the sign flipped, as under-provisioning. Verified against every case the original formula existed to serve: 8 lanes nothing claimed `0+(8-0)=8`; 8 lanes 3 claimed 5 warming `8+(5-5)=8`; linear warming `1+(1-1)=1` (was 2, the defect); linear claimed `1+(0-0)=1`; burst arriving later `1+(7-0)=8`. *Alternatives rejected*: `max(active, ready)` reintroduces the measured off-by-one that left an 8-lane scatter at 7; capping at 1 for linear flows requires the pool to know the flow shape, which it deliberately does not; lengthening `demand_recheck_seconds` widens the window the defect needs rather than closing it. **Also corrected a metric that has never measured what it claims**: `PoolResult.peak_concurrency` documented itself as *"Highest number of workers simultaneously executing nodes"* and is `max(len(_active))`, counted from `_spawn` — before the thread starts, before the worker is constructed, before any claim. Before this fix a linear flow could therefore report `peak_concurrency=5` for one task executed once. **Docstring corrected; the number deliberately left as-is** rather than silently redefined, because it is quoted in four artifacts and a live run log and a published metric that changes meaning without notice is worse than one correctly labelled. Raised as its own register entry recommending a separate `peak_lanes_executing` computed from interval overlap. **The 8-lane width claim is unaffected and this was verified rather than assumed**: `tests/test_scatter_concurrency.py` uses `threading.Barrier(8)`, which *deadlocks* rather than degrading if the pool cannot get eight lanes in flight together, and it passes — 167 tests across `test_scatter_concurrency`, `test_swarm_pool` and `test_flow_pool_integration` all green. Added `tests/test_demand_overprovisioning.py` (8 tests), whose reproduction is **forced rather than waited for**: `SlowToStartWorker` delays in `__init__`, not in `execute_cycle`, because the defect concerns the gap between slot reservation and claiming — a delay in `execute_cycle` models a slow *node* and does not reproduce this at all. **The most important class in that file is `TestTheFixDoesNotCostConcurrency`**, because capping the pool at one worker would satisfy every guard on the defect and destroy the entire Phase 6.12 deliverable; two tests assert a genuine 8-task burst still reaches width and that work arriving *after* a claim still scales up. `test_no_decision_double_counts_an_unclaimed_worker` asserts the **cause** rather than the symptom via the decision record, so a future change that masks the arithmetic still fails. **Over-provisioning was never double execution**, asserted separately because it sounds like a correctness defect and is not one — the atomic claim was always the authority and the losers retired; the cost was wasted construction and lock contention. Recording that distinction is what kept the fix proportionate. Gate observed 2026-09-04: `omni clean` 05:59, `omni qa` PASS whole project 06:14, **792 collected / 792 passed** 186.65s at 06:18 and again 187.98s at 11:05, `omni smoke` ALL CHECKS PASSED 06:20 (run because `swarm_pool` is on an execution path). **NOT VERIFIED: no live 8-lane scatter since this change** — the width proof is deterministic and the smoke test is single-node, but neither is a live multi-lane run, which is exactly the combination that was green while six real defects sat in this path. Also unproven that this was the *only* cause of the observed failure: it is a sufficient explanation, reproduced, holding across two full runs, but the original failure occurred once in six runs so absence of recurrence is weak evidence alone — the decision record now makes a recurrence diagnosable rather than mysterious, which is the more durable outcome. **Consequence for Phase 4.99: UT-0 is unblocked**, having been blocked because the demand estimator is the component UT-0 exists to measure and it held an unexplained non-determinism. Artifact: `.oracle_artifacts/2026-09-04_demand_estimator_over_provisioning.md`.
- **2026-09-04 (Requirement 33 — Pre-Launch Validation: temporal paradox detection and the total-sum readout)**: Implemented the two pre-launch mechanisms the operator asked for when settling the topological semantic — a refusal when the configured waits describe an ordering **no execution can satisfy**, and a *total-sum* readout of the whole Active Flow rather than the node-by-node view the TUI already gives. `detect_temporal_paradox` + `ParadoxReport` + `_find_cycles` + `_qualify` in `topology_graph.py`; `total_sum_readout` in `flow_engine.py`, inserted before `class FlowStep`. Both are pure functions taking no shared mutable state, deliberately: pre-launch validation runs before any of the concurrency machinery is constructed, so the observer rule has nothing to bind to. **The paradox check is ONE precedence-graph cycle detection, not the four detectors Requirement 33.2 enumerates.** Two of the four conditions are the same thing once both kinds of ordering constraint are written as edges — `node[i] → node[i+1]` for order within a lane, `target → waiter` because a waiter cannot precede its target. A lane `[W, B]` where `W` waits on `B` yields `W→B` and `B→W`, a two-node cycle; two lanes waiting on each other yield the same shape with no sequence edges at all. One cycle detection covers both **and covers three-lane and longer cycles nobody enumerated**, which is the whole argument for deriving the check from the model rather than from the requirement's list of examples: four special-case detectors would have passed every test written from the requirement text and missed the shapes the requirement did not think of. The remaining two conditions are reference-validity errors rather than ordering contradictions and are reported in a separate `unresolvable` field with a reason string, so a refusal can say *which kind* of wrong the topology is. `_find_cycles` is **iterative with an explicit stack, recursion rejected** — this validator runs on operator input immediately before launch, and a validator that raises `RecursionError` converts "your config is unsatisfiable" into "the tool crashed", which is worse than the defect it exists to catch. An unresolvable target is **excluded from the graph rather than graphed against nothing**, because inventing a graph node for a nonexistent target could manufacture a phantom cycle — Principle 2, an approximately-correct identifier acted upon. `participants` is an order-preserving de-dup and not a set, since a set makes the refusal message non-deterministic and the tests flaky for free. **The readout's `source` field is CHECKED, not asserted**: Requirement 33.6 says it derives from the hydrated topology and never the authoring surface, and hardcoding `source = "hydrated_topology"` would have made 33.6 decorative, so the function tests whether every `Node_ID` carries the `_S{step_index}` suffix `_hydrate_topology` applies and reports `"unhydrated_topology_rows"` plus a `logger.warning` when it does not — Principle 4 in the one artifact whose entire job is to tell the operator the truth before they commit, and the TUI/engine `NAME_{i}` vs `NAME_S{i}` split is exactly what a readout built from what was *drawn* would have reproduced. Unbuilt fields are **empty rather than fabricated** (`gather_strategies={}`, `waits={}`, `cross_lane_routes=[]`, for Reqs 29/31/32 which are specified and unbuilt): the keys exist so consumers have a stable shape, and a plausible-looking default would be Principle 3 in a readout. **`expected_peak_concurrency` was wrong on first implementation and a test caught it, which is worth recording because both sides were wrong in different ways.** The first version computed `resolve_scatter_cap(lane_count)`; the test asserted a 64-lane topology could not expect more than 8-way and failed with **12**. Reading the engine settled it: `execute_step` passes `max_workers=len(scatter_agents)` when agents are slotted and `None` otherwise, and `DynamicSwarmPool` clamps *that*, so an unconfigured step peaks at `MAX_SCATTER_AGENTS` (8) and **not** `SCATTER_HARD_CAP` (12) — and the lane count was never the pool's input, so resolving it as one was a second derivation of the sizing rule, Principle 4 again. A 64-lane topology would have read as "12-way" while the run opened 8 threads: **a readout over-promising concurrency the engine was never going to deliver, in the one artifact the operator consults *instead of* watching the run.** Fixed by giving the readout the same input the pool gets — `min(lane_count, resolve_scatter_cap(max_workers)) if lane_count else 1`, bounded twice over since you cannot run more lanes at once than exist — and the test now asserts against the imported constants rather than literals, so a change to either ceiling cannot leave it passing for the wrong reason. *Rejected: keeping `resolve_scatter_cap(lane_count)` and relaxing the assertion to `<= SCATTER_HARD_CAP`*, which would have gone green while leaving the over-promise in place. A linear flow reports `lane_count == 0` and a peak of 1, distinguishing "no lanes" from "one lane" because the second is a scatter of width one and the first is not a scatter. Added `tests/test_prelaunch_validation.py` (26 tests, in four groups: paradox shapes with the satisfiable negative case first, reference validity, refusal quality, and the readout), and **removed the 5 `xfail(strict=True)` markers standing in for Requirement 33 in `test_topological_semantic_spec.py` — all five XPASSed**, which is the strict-xfail mechanism doing its job: an XPASS is a failure, so the markers could not be left behind by accident. Gate observed 2026-09-04: `omni clean` 20:16 (zombie hunt: nothing to terminate, 55 bytecode files purged), `omni qa` **PASS whole project** 20:17, **869 collected / 853 passed / 16 xfailed / 0 failed** 231.94s, `omni smoke` **ALL CHECKS PASSED** 21:00 at $0.00 (run because both modified files sit on execution paths). Collected count 843 → 869 reconciles exactly: +26 new tests, and 5 previously-xfailed spec tests moved into the passing count (21 → 16 xfailed), so `853 = 822 + 5 + 26`. **NOT VERIFIED, and it is the important limit: nothing calls either function yet.** Both are implemented, covered and **unwired** — no launch path invokes them, so no operator currently sees a refusal or a readout, and "Requirement 33 is implemented" and "MACCRE refuses paradoxical configs at launch" are different claims of which only the first is true today. Wiring is tracker #13 (TUI) and tracker #11 (`CTRL_WAIT`). Relatedly, **`waits` has no producer**: the argument is shaped for `CTRL_WAIT` configuration and `CTRL_WAIT` is still a `ComingSoon` registry row, so every wait in the tests is hand-constructed. And `paradox=False` is a statement about the two inputs given, **not** a guarantee the flow will succeed. Artifact: `.oracle_artifacts/2026-09-04_prelaunch_validation.md`.
- **2026-09-04 (Requirement 30 — a step's output is a set, with Requirement 29's enum pulled forward)**: A step used to produce *an artifact*, which was true only while every step had one endpoint and stopped being true the moment Req 29 allowed a scatter to leave its lanes ungathered. Req 30 makes the multi-output case **normal** and moves the error from "there are several" to **"something chose between them without being told to"**. Added `GatherStrategy` (Merge/Concat/Ungathered) to `deterministic_nodes.py` beside `DeterministicNodeType`; `DECLARED_TOPOLOGY_POSITION`, `StepOutputSet`, `resolve_gather_strategy`, `step_declares_a_gather_strategy` and `FlowRunner._collect_step_output_set` to `flow_engine.py`; `self._step_output_sets` to `FlowRunner.__init__`; and `step.config` now passed at all three `_capture_step_output` call sites. **Req 29's enum came forward because 30.3 and 30.4 are both phrased "AND the step's Gather Strategy is …" and there is no honest way to implement 30.4 — the clause the whole amendment turns on — without the thing it branches on.** Exactly two pieces (29.2 the enum, 29.6 the `Merge` default) and no more: 29.3's launch-time `validate_gather_reachability` and 29.4's `terminal_outputs_for_step` stay `xfail(strict=True)` for tracker #11, because half-building the validator would leave a mechanism nobody could tell was incomplete. **`ordered_by` is a property, not a field**, so it cannot be constructed as `StepOutputSet(pairs=…, ordered_by="completion_time")` — a caller able to *state* an ordering it did not perform is the shape of every label defect in this register (`is_stalled = True` for a timeout; `payload_path` naming the shared ledger), and the ordering is a property of how the object is built rather than the constructor's to declare. **`single()` raises on both empty and crowded, with non-overlapping messages** (`"no output"` vs `"more than one"`): "several outputs and no instruction" is an authoring question and "no output at all" is a run failure, they call for different responses, and folding them into one error is the ambiguous-terminal-state problem in miniature. **`substitute_guess()` always returns `None` and is documented as a canary, not a method** — E2's whole failure mode was a helper that, asked for a step's output, produced a *plausible* one rather than nothing; a no-fallback rule is otherwise an absence and an absence cannot be asserted, so naming it forces a future fallback to come through it and redden a test instead of arriving as an innocuous default. **`pairs` is copied on construction**, since the Oracle principles name mutating a caller's list as a bug pattern and this object exists to be a stable record — a later reorder of the caller's list must not rewrite history. **An unrecognised Gather Strategy is refused, never defaulted**: `resolve_gather_strategy` returns `Merge` for absent/blank only and raises `ValueError` for a declared-but-unknown value, because quietly reading `"ungatherd"` as `Merge` would gather lanes the author explicitly asked to leave alone — the approximately-correct value in its most expensive form, since the wrong behaviour is indistinguishable from the right one until somebody reads the merged document. Resolution is case-insensitive (the authoring surface is a text field; casing is not a declaration), and `_capture_step_output` catches the `ValueError` and refuses the step rather than letting a running flow die at step 4 on an authoring typo — the launch-time refusal it really deserves is 29.3's job. **THE DECISION THAT TOOK LONGEST, AND THE ONE MOST LIKELY TO BE REVISITED: a step with no scatter has no Gather Strategy.** The `Merge` default (29.6, so saved MacroNodes keep their behaviour) applied unconditionally would reach a **plain divergent DAG** — `ROOT → L1,L2`, both terminating, two endpoints and no lanes — which would resolve to `Merge`, find no merge node, and be refused. That changes the behaviour of topologies Requirement 30 says nothing about, *in the name of implementing Requirement 30*. So `step_declares_a_gather_strategy` gates the entire strategy branch on the presence of a `CTRL_SCATTER` (by prefix, matching `_resolve_node_type`, so `CTRL_SCATTER_WIDE` counts), and for the plain divergent DAG the pre-amendment behaviour stands unchanged: first in declared order with the warning that has always said it is *a choice, not a fact*. **The two existing `test_payload_lineage.py` tests that pin that behaviour still pass untouched, which is the evidence this change did not reach behaviour it was not meant to reach.** `TestAPlainDivergentDagIsUntouched` guards it from the other side, so extending 30.4's refusal there has to arrive as a spec change that reddens the class deliberately; raised as a register entry rather than decided. Three further branch decisions: **`Ungathered` with one lane is still the degenerate case** (one output, nothing to choose between, and refusing would break the legitimate width-one scatter that exists to get a tether scope without fanning out); **`Merge`/`Concat` with no gather output refuses**, being the unreachable gather 29.3 exists to catch pre-launch, where the runtime alternative is handing the next step one lane of several; and **two gather nodes with output also refuses**, an authoring error rather than a choice for the engine. `_capture_step_output` still returns `str | None`, and **`None` now covers five conditions** (no terminal declared, no terminal recorded, `Ungathered` multi, declared-gather-absent-or-doubled, unresolvable declaration), each logged with which one it was — accepted as a cost and recorded rather than glossed, since callers treat `None` uniformly as "carry the previous payload", which is the same conservative behaviour as before, and the distinction lives only in the log. **Handing the set itself to the next step is deliberately NOT done here**: that is tracker #9's payload contract, and widening the return type before that design exists would produce the second representation the payload work is meant to eliminate. Added `tests/test_step_output_set.py` (44 tests across eight groups) and removed **6** `xfail(strict=True)` markers (29.2, 29.6, 30.1, 30.2, 30.4, 30.5) after confirming all six XPASSed. **Revert-to-red proof**: the `Ungathered` branch was temporarily changed to `return output_set.paths()[0]`, which reddened two tests with the production signature `assert '/dc/Agent1_S0.md' is None` — one lane of eight wearing the whole step's name — probe then removed and confirmed absent by grep before re-running green. **A finding from that probe worth keeping: `test_the_refusal_is_logged_at_error_with_the_count` STILL PASSED under it**, because the ERROR line was emitted while the value was returned anyway — the log-says-one-thing-code-does-another shape, meaning the log assertion is not evidence of the refusal and the two value assertions are what carry the claim. Gate observed 2026-09-04: `omni clean` 21:24 (nothing to terminate, 284 bytecode files purged), `omni qa` **PASS whole project** 21:25, **913 collected / 903 passed / 10 xfailed / 0 failed** in 205.34s, `omni smoke` **ALL CHECKS PASSED** at $0.00 (run because `flow_engine` and `deterministic_nodes` are both execution paths). Collected 869 → 913 reconciles exactly: +44 tests, 6 markers removed (16 → 10 xfailed), so `903 = 853 + 6 + 44`. **NOT VERIFIED, and the limit is sharp: nothing authors a Gather Strategy.** `step_config["gather_strategy"]` has **no producer** — the TUI does not offer the field — so every declaration in the tests is hand-constructed and every real flow today resolves to the `Merge` default. The runtime behaviour of every existing topology is therefore unchanged, which is intended and also means the new branches are unproven where they matter most; `Ungathered` has never executed against a real scatter, and there has been no live multi-lane run. Req 30.6's audit trail is `_step_output_sets` in-process plus `as_record()` logged at INFO into `maccre_system.log`; it is **not** persisted to the queue or telemetry, so "auditable after the run" holds only as far as the log does. Artifact: `.oracle_artifacts/2026-09-04_step_output_as_a_set.md`.
- **2026-09-04 (The payload mode seam — tracker #9 part one, plus two defects found by mapping the contract)**: Task #9 is "the payload contract as one design", which the register defines as three things: (a) `Payload_Mode` honoured at a step boundary, (b) `Preceding Node Only` actually implemented, (c) the HITL injection accompanying rather than replacing the payload. **This landed the foundation and (b), and deliberately stopped short of (a) and (c)** — those are one semantic decision that changes what every existing multi-step flow passes *and costs*, the register records arguments both ways plus a recommended middle option, and it is the operator's call rather than mine. Everything built here is needed under every possible answer to it. **The map came first, and the important fact was how few sites mattered: exactly TWO behavioural reads exist in the whole tree, both in `swarm_worker`, against ~25 writes across six other files.** `swarm_worker` ~L1023 reads the **completing** node's mode, tests only for `Targeted Filter`, and rewrites what *that node itself* reads; ~L1874 reads the **successor's** mode, tests only for `Unified Ledger`, and rewrites what the *next* node reads. **Two modes on one field with opposite subjects** — the single most confusing thing about this contract, stated nowhere until now, and now documented on the enum members. New `maccre_core/orchestration/payload_modes.py` holds `PayloadMode` (three members), `DEFAULT_PAYLOAD_MODE`, `AUTHORABLE_MODES` and `resolve_payload_mode`; it imports **only the standard library**, deliberately, because `topology_engine` is the seam every read passes through and `flow_engine` imports `topology_engine`, so hosting the enum in `flow_engine` would have made the seam import its own consumer. **THE ONE BEHAVIOUR CHANGE, recorded as one rather than filed as cleanup: a typo used to select a different contract.** Both reads were `==` against a literal, so a topology carrying `"Unifed Ledger"` did not fail, did not warn, and **routed as `Preceding Node Only`** — because the override tested equality and the else-branch was the entirety of the other mode. One transposed letter silently changed which document the next agent read. Resolution is now case-insensitive against the three known values with a fallback to the default **and a warning naming the node**, so that topology now routes as `Unified Ledger`: differently than before, and as authored. **A deliberate asymmetry with `resolve_gather_strategy`, written a day earlier, which RAISES on an unrecognised value while this one WARNS:** raise when the default could silently do the opposite of what was asked (defaulting an unknown Gather Strategy to `Merge` would gather lanes the author asked to leave alone); warn when the default lands on the likely intent and the code is on the worker's hot path where a topology typo is not worth killing a running flow. **(b) `Preceding Node Only` is now an explicit branch that changes nothing.** It was offered in the UI and read by no conditional anywhere — it worked by falling through, since when the mode was not `Unified Ledger` the override did not fire and `routing_payload_path` stayed at the completing node's own artifact, which for an intra-step hop *is* preceding-node-only. Accidentally correct, and the dangerous kind: **defect E1 was exactly a change to the default routing path with unexamined consequences for a mode nobody had written down.** The new branch logs and does nothing else, and `test_the_branch_does_not_change_the_routing_payload` asserts there is no assignment in its body, because a behaviour change wearing a clarification's clothes is precisely what must not happen here. What it buys is that the mode can be **conditioned on**, which the operator's stated `CTRL_REVIEW` design requires and a fall-through cannot provide. **Also fixed, and it is defect F1's class exactly: the config modal could be handed a `Select` value its options excluded.** `Targeted Filter` is written onto consensus advocate rows by `macro_factory` and is not offered by the modal, so opening the modal on such a node supplied a `value` absent from the option list — decided at construction, inside a `compose`, where a raise is an app-killing traceback rather than a message. Options now derive from `AUTHORABLE_MODES` and a non-authorable current value is appended labelled `(not authorable)`: two modes offerable, three renderable. **THE SECOND DEFECT, and it is the more consequential one: the HITL resume never passed its topology.** `resume_paused_task` takes a `topology_engine` so a paused *pause node* can be completed and routed to its configured successor, falling back to `"END"` when omitted — and `_hitl_resume_with_context`, **the only production caller**, omitted it. So every HITL resume from the TUI closed the lane at the pause node and silently dropped whatever the operator had authored after it, while the parameter's own docstring warned about that exact failure in detail. **The `--smart` shape from the doctrine: a documented mechanism with no caller supplying it and no test that fails when the claim goes false.** **Verified rather than assumed that a plain `TopologyEngine` suffices**: the concern was that a control node's successor is config-driven through `merge_config_overlay`, which never touches `topology.csv`, so a fresh CSV-reading engine might resolve the wrong successor — but reading `_get_macronode`'s control-node auto-wrap settled it, because `next_node = str(cfg.get("next_node", "END"))` is written **into the topology row** and `_hydrate_topology` writes that row to the CSV with the step suffix. The overlay carries the *other* config fields (scatter targets, gate predicates, `auto_resume_after`), not the successor, so the CSV is authoritative for this lookup. A defect fix, not a semantic choice: the intended behaviour is documented on the parameter and the fallback is what was wrong. Added `tests/test_payload_modes.py` (35 tests), including the typo change driven **functionally through a real `TopologyEngine`** over a four-row CSV (valid / typo / blank / wrong-case) rather than by unit-testing the resolver alone, and a parametrised `TestTheLiteralsAreGone` across all seven files plus `test_every_migrated_file_reads_through_the_seam` guarding the other direction — a file with neither a literal nor an import would be a file that lost the concept, with `undo_manager` the deliberate exception since it never named a mode. **Source-level assertions earn their place here specifically because the reads were equality tests, so a drifted literal was undetectable at runtime**; the defect being prevented is a fourth spelling appearing in an eighth file, which is a property of the source. **Revert-to-red**: reverting `topology_engine`'s resolve call to the old literal expression reddened three tests — `assert 'X' == 'Unified Ledger'` for typo and blank, `assert 'unified ledger' == 'Unified Ledger'` for casing — probe removed and grep-confirmed before re-running green. Gate observed 2026-09-04: `omni clean` 21:57 (nothing to terminate, 286 bytecode purged), `omni qa` **PASS whole project** 21:58, **948 collected / 938 passed / 10 xfailed / 0 failed** in 209.52s, `omni smoke` **ALL CHECKS PASSED** at $0.00. Collected 913 → 948 is +35 exactly, and the pass count moved by the same 35, so **nothing that was already passing changed behaviour under test** — the claim this migration most needed to support. **NOT VERIFIED: no live run.** Both behaviour changes are covered by tests and unproven in production, and the HITL one is TUI wiring, which sits outside the Pyright include list — `test_the_tui_supplies_it` is a source assertion, not an execution. **Two findings recorded and deliberately NOT fixed, because the right answer to each depends on decision (a):** a fan-out consults only lane 0's payload mode (`next_node.split(",")[0]`, so lanes with differing modes all get lane 0's); and `payload_mode` is per-node in the CSV but **per-step in the flow model**, since `_hydrate_topology` stamps one `FlowStep.payload_mode` onto every row of the macro, so a TUI-authored step cannot vary mode between its own nodes while `macro_factory` rows can. Artifact: `.oracle_artifacts/2026-09-04_payload_mode_seam.md`.
